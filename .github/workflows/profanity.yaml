# The name of the workflow
name: Profanity filter

# Trigger on issue or pull requests, that are opened, edited, or reopened
on:
  issue_comment:
    types: [created, edited]
  issues:
    types: [opened, edited, reopened]
  pull_request:
    types: [opened, edited, reopened]

# Required permissions
permissions:
  issues: write
  pull-requests: write

jobs:
  # Name the job whatever you'd like
  apply-filter:

    runs-on: ubuntu-latest

    steps:

    # Name the step anything that makes sense to you
    - name: Scan issue or pull request for profanity
      # Conditionally run the step if the actor isn't a bot
      if: ${{ github.actor != 'dependabot[bot]' && github.actor != 'github-actions[bot]' }}
      uses: IEvangelist/profanity-filter@main
      id: profanity-filter
      with:
        token: ${{ secrets.PROFANITY_TOKEN }}

        # please.
        include-confused-reaction: true

        # See https://bit.ly/potty-mouth-replacement-strategies
        replacement-strategy: redacted-rectangle

        # Sourced from:
        # https://github.com/coffee-and-fun/google-profanity-words
        custom-profane-words-url: https://raw.githubusercontent.com/coffee-and-fun/google-profanity-words/refs/heads/main/data/en.txt
